<Window x:Class="ShopOrdersWpf.EditOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Редактирование заказа"
        Height="250"
        Width="350"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Название товара:"/>
            <TextBox Text="{Binding Product}" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Количество:"/>
            <TextBox Text="{Binding Quantity}" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Статус:"/>
            <ComboBox SelectedItem="{Binding Status}" Margin="0,5,0,0">
                <ComboBox.Items>
                    <sys:String>Новый</sys:String>
                    <sys:String>Доставляется</sys:String>
                    <sys:String>Доставлен</sys:String>
                </ComboBox.Items>
            </ComboBox>
        </StackPanel>

        <Button Grid.Row="3"
                Content="Сохранить"
                HorizontalAlignment="Right"
                Width="100"
                Click="Save_Click"/>
    </Grid>
</Window>